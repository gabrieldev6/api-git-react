import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
//codigo foi modificado, devido atualizacao na sintax
const root = ReactDOM.createRoot(document.getElementById('root'))
root.render(
  <React.StrictMode>
  <App />
  </React.StrictMode>
)

 
